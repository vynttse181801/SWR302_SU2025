{
  "name": "datetime-checker-tests",
  "version": "1.0.0",
  "description": "API Tests for DateTime Checker",
  "scripts": {
    "test": "npx codeceptjs run --steps",
    "test:ui": "npx codeceptjs run --steps --ui"
  },
  "devDependencies": {
    "@codeceptjs/ui": "^0.7.7",
    "codeceptjs": "^3.5.13-beta.3",
    "webdriverio": "^8.0.0"
  }
}
